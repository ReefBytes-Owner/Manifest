"""Thin native hook adapters: `manifest hook <client> <event>`.

See docs/SHARED_CHECKS_HOOKS.md. Nothing here has been verified against a
real client; every adapter receipt carries `client_version_verified: false`.
"""
