# Claude Code hook fixtures — source: "unverified"

Derived from this repository's own
`plugins/manifest-workspace/skills/ai-hooks-integration/references/schemas/claude-hooks.schema.json`
and `references/contracts/claude-{pretooluse,posttooluse,stop}-contract.yaml`,
and `configs/claude/scripts/constitution_hook.py`'s stdin-parsing pattern.

**Not verified against a real Claude Code client at any pinned version.**
`client_version_verified` is `false` on every receipt this adapter writes.
See docs/SHARED_CHECKS_HOOKS.md.
