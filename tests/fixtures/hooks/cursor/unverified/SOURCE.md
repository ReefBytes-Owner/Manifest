# Cursor CLI hook fixtures — source: "unverified"

Derived from this repository's own
`plugins/manifest-workspace/skills/ai-hooks-integration/references/schemas/cursor-hooks.schema.json`
and `references/contracts/cursor-beforeShellExecution-contract.yaml` — the
only Cursor hook this repository documents. Every other event name is
`{"coverage": "unsupported"}`, never emulated.

**Not verified against a real Cursor client at any pinned version.**
`client_version_verified` is `false` on every receipt this adapter writes.
